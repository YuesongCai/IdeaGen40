"""Feed implementations. Every module here is imported by the registry.

Adding a source is adding a file that returns rows in its kind's shape. Nothing
downstream changes, because the shape is what downstream depends on.
"""
